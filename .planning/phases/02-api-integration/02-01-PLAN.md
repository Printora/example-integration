---
phase: 02-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/printora-client.ts
  - app/api/printora/session/route.ts
  - app/create-session/page.tsx
  - lib/actions/create-session.ts
autonomous: true
user_setup:
  - service: printora
    why: "API key for session creation"
    env_vars:
      - name: PRINTORA_API_KEY
        source: "Printora Dashboard (staging: sk_test_yulieko-favourse-com_6b7d1b23590d9796a48eec0a0694b19c)"
      - name: PRINTORA_API_URL
        source: "Default: https://api-staging.printora.ai"

must_haves:
  truths:
    - "User can submit form with design image URL and customer data, and system creates a Printora session"
    - "User is redirected to Printora design editor after successful session creation"
    - "Failed API calls return clear error messages to the user"
  artifacts:
    - path: "lib/printora-client.ts"
      provides: "Typed fetch wrapper for Printora API with error handling"
      exports: ["createSession", "PrintoraApiError"]
    - path: "app/api/printora/session/route.ts"
      provides: "POST endpoint for session creation"
      exports: ["POST"]
    - path: "lib/actions/create-session.ts"
      provides: "Server Action for form handling with Zod validation"
      exports: ["createSessionAction"]
    - path: "app/create-session/page.tsx"
      provides: "Form page with useActionState for state management"
  key_links:
    - from: "lib/printora-client.ts"
      to: "env.PRINTORA_API_KEY"
      via: "Bearer token in Authorization header"
      pattern: "Authorization.*Bearer.*PRINTORA_API_KEY"
    - from: "lib/printora-client.ts"
      to: "https://api-staging.printora.ai"
      via: "fetch API call"
      pattern: "fetch.*PRINTORA_API_URL"
    - from: "app/create-session/page.tsx"
      to: "/api/printora/session"
      via: "Server Action calling API route"
      pattern: "createSessionAction"
    - from: "lib/actions/create-session.ts"
      to: "lib/printora-client.ts"
      via: "Direct import and function call"
      pattern: "import.*createSession.*from.*printora-client"
---

<objective>
Create Printora API client with session creation functionality and a form-based session creation flow.

**Purpose:**
Enable users to submit a form with design image URL and customer data, create a Printora session via API, and redirect to the Printora design editor.

**Output:**
- `lib/printora-client.ts` - Typed fetch wrapper with error handling
- `app/api/printora/session/route.ts` - POST endpoint for session creation
- `lib/actions/create-session.ts` - Server Action with Zod validation
- `app/create-session/page.tsx` - Form page using React 19 useActionState
</objective>

<execution_context>
@C:/Users/yulie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yulie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-security/01-01-SUMMARY.md
@.planning/phases/01-foundation-security/01-03-SUMMARY.md
@.planning/phases/02-api-integration/02-RESEARCH.md

@lib/env.ts
@types/printora.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Printora API client with typed fetch wrapper</name>
  <files>lib/printora-client.ts</files>
  <action>
Create `lib/printora-client.ts` with:

1. **PrintoraApiError class** - extends Error with status, code, message properties
2. **createSession function** - async function accepting PrintoraSessionRequest, returns PrintoraSessionResponse
3. **API endpoint constant** - use `/partner/sessions` as path (configurable via PRINTORA_SESSION_PATH env var if needed)
4. **Fetch implementation** using native fetch with:
   - Authorization: `Bearer ${env.PRINTORA_API_KEY}`
   - Content-Type: application/json
   - POST to `${env.PRINTORA_API_URL}/partner/sessions`
5. **Error handling** - check response.ok (fetch doesn't throw on 4xx/5xx):
   - 401: throw PrintoraApiError with AUTH_FAILED code
   - 400: throw PrintoraApiError with VALIDATION_FAILED code
   - Other errors: generic message with status code
6. **Type imports** from @/types/printora

Import env from @/lib/env for PRINTORA_API_KEY and PRINTORA_API_URL.

Per research: "Fetch doesn't throw on HTTP errors - must check response.ok"
  </action>
  <verify>
TypeScript compilation passes: `npx tsc --noEmit`
File exports: createSession, PrintoraApiError
  </verify>
  <done>
PrintoraApiError class exists with status, code, message
createSession function accepts PrintoraSessionRequest
Function throws PrintoraApiError on non-2xx responses
  </done>
</task>

<task type="auto">
  <name>Task 2: Create POST /api/printora/session endpoint</name>
  <files>app/api/printora/session/route.ts</files>
  <action>
Create `app/api/printora/session/route.ts` with:

1. **POST handler** accepting { imageUrl, userData: { name, email }, successUrl, failedUrl }
2. **Zod validation schema** for request body:
   - imageUrl: z.string().url("Invalid URL format")
   - userData.name: z.string().min(1, "Name is required")
   - userData.email: z.string().email("Invalid email address")
   - successUrl: z.string().url().optional()
   - failedUrl: z.string().url().optional()
3. **Validation flow**:
   - Parse request body with req.json()
   - Validate with schema.safeParse()
   - Return 400 with field errors if invalid
4. **Default URLs** - derive successUrl/failedUrl from env.NEXT_PUBLIC_APP_URL if not provided:
   - successUrl: `${env.NEXT_PUBLIC_APP_URL}/callback/success`
   - failedUrl: `${env.NEXT_PUBLIC_APP_URL}/callback/failed`
5. **Call createSession** from @/lib/printora-client
6. **Return NextResponse.json** with { sessionId, editorUrl, expiresAt }

Import: { NextResponse } from 'next/server', { z } from 'zod', { createSession } from '@/lib/printora-client', { env } from '@/lib/env'

Per research: "Always return NextResponse explicitly, not undefined"
  </action>
  <verify>
TypeScript compilation passes
Endpoint responds to POST with valid body
Returns 400 for invalid input with error fields
  </verify>
  <done>
POST endpoint accepts JSON body
Validates inputs with Zod schema
Returns 200 + session data on success
Returns 400 + error details on validation failure
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Server Action for form handling</name>
  <files>lib/actions/create-session.ts</files>
  <action>
Create `lib/actions/create-session.ts` with:

1. **'use server'** directive at top of file
2. **createSessionAction** function with signature:
   ```typescript
   export async function createSessionAction(
     prevState: FormState,
     formData: FormData
   ): Promise<FormState>
   ```
3. **FormState interface**:
   ```typescript
   interface FormState {
     success: boolean
     errors?: Record<string, string[]>
     editorUrl?: string
   }
   ```
4. **Zod validation** using same schema as API route
5. **Extract values** from formData: imageUrl, name, email
6. **Build userData** object: { name, email }
7. **Derive URLs** from env.NEXT_PUBLIC_APP_URL (note: use client-side env pattern)
8. **Call /api/printora/session** via fetch (server-side)
9. **Return state**: { success: true, editorUrl } on success, { success: false, errors } on failure

Per research: "Server Actions provide simpler form handling with automatic pending states"
  </action>
  <verify>
TypeScript compilation passes
'use server' directive present
Function signature matches FormState pattern
  </verify>
  <done>
createSessionAction exported with 'use server' directive
Validates form data with Zod
Returns FormState with success flag
  </done>
</task>

<task type="auto">
  <name>Task 4: Create session creation form page</name>
  <files>app/create-session/page.tsx</files>
  <action>
Create `app/create-session/page.tsx` with:

1. **'use client'** directive (required for useActionState)
2. **Imports**: { useActionState } from 'react', { useRouter } from 'next/navigation', createSessionAction from '@/lib/actions/create-session'
3. **Component** SessionCreateForm:
   - Call useActionState(createSessionAction, initialState)
   - Extract [state, formAction, isPending]
   - Use useRouter for redirect on success
   - useEffect to redirect when state.editorUrl exists
4. **Form HTML**:
   - Input: imageUrl (type="url", required, placeholder="https://example.com/design.png")
   - Input: name (type="text", required, placeholder="Customer name")
   - Input: email (type="email", required, placeholder="customer@example.com")
   - Button: type="submit", disabled={isPending}
5. **Error display**: Show state.errors[field] messages in red below each input
6. **Styling**: Use Tailwind classes (form-container, input fields, button)

Per research: "React 19's useActionState hook for form state management"
  </action>
  <verify>
Page loads without errors
Form submits without page reload
Redirect occurs to editorUrl on success
  </verify>
  <done>
Page renders at /create-session route
Form accepts imageUrl, name, email
Shows validation errors from Server Action
Redirects to Printora editor on success
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes: `npx tsc --noEmit`
2. Form page loads at /create-session
3. Form validates invalid email with error message
4. Valid form submission creates session and redirects
5. API errors (401, 400) display user-friendly messages
</verification>

<success_criteria>
1. User can navigate to /create-session and see the form
2. Form validates input and shows errors for invalid data
3. Successful submission creates Printora session
4. User is redirected to Printora editor URL after session creation
5. API authentication failures return clear error messages
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-integration/02-01-SUMMARY.md`
</output>
