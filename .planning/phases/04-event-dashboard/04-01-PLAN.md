---
phase: 04-event-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components/dashboard/event-filter.tsx
  - components/dashboard/event-search.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "ahooks and react-json-view-lite packages are installed"
    - "EventFilter dropdown allows selecting event types (all, order.created, order.paid, order.shipped, order.delivered)"
    - "EventSearch input accepts search query for order ID or email"
    - "Both components expose controlled value/onChange props for parent state management"
  artifacts:
    - path: "package.json"
      provides: "Dependencies for polling and JSON display"
      contains: "ahooks"
    - path: "package.json"
      provides: "Dependencies for JSON syntax highlighting"
      contains: "react-json-view-lite"
    - path: "components/dashboard/event-filter.tsx"
      provides: "Type filter dropdown component"
      exports: ["EventFilter"]
      min_lines: 40
    - path: "components/dashboard/event-search.tsx"
      provides: "Search input component"
      exports: ["EventSearch"]
      min_lines: 30
  key_links:
    - from: "components/dashboard/event-filter.tsx"
      to: "react"
      via: "useState import for controlled component"
      pattern: "import.*useState.*from ['\"]react['\"]"
    - from: "components/dashboard/event-search.tsx"
      to: "@/components/ui/input"
      via: "Input component from shadcn/ui"
      pattern: "from ['\"]@/components/ui/input['\"]"
---

<objective>
Install required dependencies (ahooks for polling, react-json-view-lite for JSON highlighting) and create controlled filter/search components for the dashboard.

Purpose: These input components manage filter type and search query state, allowing users to filter events by type and search by order ID or email.

Output: Updated package.json with new dependencies, EventFilter and EventSearch components with controlled props.
</objective>

<execution_context>
@C:/Users/yulie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/yulie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-dashboard/04-RESEARCH.md
@types/printora.ts
@components/ui/input.tsx
@lib/utils.ts
@.planning/phases/03-ui-components/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Install dashboard dependencies</name>
  <files>package.json</files>
  <action>
    Install ahooks (for useInterval polling) and react-json-view-lite (for JSON syntax highlighting):

    ```bash
    npm install ahooks react-json-view-lite
    ```

    After install, verify package.json contains:
    - "ahooks" in dependencies
    - "react-json-view-lite" in dependencies

    Do NOT install react-json-view (heavy alternative) or @uidotdev/usehooks (ahooks is preferred per research).
  </action>
  <verify>grep -E "(ahooks|react-json-view-lite)" package.json</verify>
  <done>Both packages are present in package.json dependencies</done>
</task>

<task type="auto">
  <name>Create EventFilter component</name>
  <files>components/dashboard/event-filter.tsx</files>
  <action>
    Create controlled EventFilter component at `components/dashboard/event-filter.tsx`:

    Component requirements:
    - Props: `{ value: string, onChange: (value: string) => void }`
    - Renders a native select element with shadcn/ui styling (using cn utility)
    - Options: "all", "order.created", "order.paid", "order.shipped", "order.delivered"
    - Display labels: "All Events", "Order Created", "Order Paid", "Order Shipped", "Order Delivered"
    - Use lucide-react Filter icon for visual indicator (import from lucide-react)

    Styling pattern (consistent with existing UI):
    ```tsx
    className="flex items-center gap-2 rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"
    ```

    The component is fully controlled (parent manages state).
  </action>
  <verify>test -f components/dashboard/event-filter.tsx && grep -q "EventFilter" components/dashboard/event-filter.tsx</verify>
  <done>EventFilter component exists, exports controlled select with 5 event type options</done>
</task>

<task type="auto">
  <name>Create EventSearch component</name>
  <files>components/dashboard/event-search.tsx</files>
  <action>
    Create controlled EventSearch component at `components/dashboard/event-search.tsx`:

    Component requirements:
    - Props: `{ value: string, onChange: (value: string) => void, placeholder?: string }`
    - Uses shadcn/ui Input component (import from @/components/ui/input)
    - Uses lucide-react Search icon for visual indicator
    - Layout: Icon on left, Input on right, wrapped in flex container

    Structure:
    ```tsx
    <div className="relative flex items-center">
      <Search className="absolute left-3 h-4 w-4 text-muted-foreground" />
      <Input
        value={value}
        onChange={(e) => onChange(e.target.value)}
        placeholder={placeholder ?? "Search by order ID or email..."}
        className="pl-9"
      />
    </div>
    ```

    The component is fully controlled (parent manages state).
  </action>
  <verify>test -f components/dashboard/event-search.tsx && grep -q "EventSearch" components/dashboard/event-search.tsx</verify>
  <done>EventSearch component exists, uses Input from shadcn/ui with Search icon</done>
</task>

</tasks>

<verification>
1. Run `npx tsc --noEmit` - no TypeScript errors
2. Verify both components export default and named exports
3. Check components use controlled pattern (value/onChange props)
</verification>

<success_criteria>
- Dependencies installed: ahooks and react-json-view-lite in package.json
- EventFilter component exists with 5 filter options (all + 4 event types)
- EventSearch component exists using Input from shadcn/ui
- Both components follow controlled component pattern with value/onChange props
- TypeScript compilation passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-event-dashboard/04-01-SUMMARY.md`
</output>
